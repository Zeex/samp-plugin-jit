sampjit is a JIT plugin for SA-MP server. 

sampjit transforms AMX bytecode into x86 native code at run-time
to achieve maximum execution speed.

The JIT compiler should support usual code produced by the SA-MP 
Pawn compiler, but is not guaranteed to work with code contains
various #emit hacks.
